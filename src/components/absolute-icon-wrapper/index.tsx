import styled from 'styled-components'

export const AbsoluteIconWrapper = styled.div`
  position: absolute;
  top: 15px;
  right: 15px;

  & > svg {
    width: 20px;
    height: 20px;
    cursor: pointer;
  }
`
