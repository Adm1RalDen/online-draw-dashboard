import styled from 'styled-components'

export const RecoverPasswordWrapper = styled.section`
  min-height: 100vh;

  & > div {
    min-height: inherit;
    display: flex;
    align-items: center;
    justify-content: center;
  }
`
